tests: crate trybuild
docs: https://docs.rs/syn/latest/syn/#parsing-a-custom-syntax

Lustre: https://homepage.cs.uiowa.edu/~tinelli/classes/181/Spring10/Notes/03-lustre.pdf

Feature: clippy_allow
Feature: warning if #[export] without #[doc]
Feature: #[test]
lint plugin ?

IMPORTANT: check that we don't have an issue of `->` with Flip going at the wrong speed.
We're going to need the clocks to be *identical* not compatible.
