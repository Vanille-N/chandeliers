chandeliers_lus::decl! {
    node foo() returns ();
    var t : bool;
    let
        t = not pre not not pre (pre not not true or not not (pre float(1) > pre - pre pre - - - - 0.0));
    tel
}

fn main() {}
